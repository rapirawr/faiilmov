<IfModule mod_rewrite.c>
    Options -Indexes
    RewriteEngine On

    # Block direct access to repo directory
    RewriteRule ^faiilmov-repo - [F,L]

    # Redirect all traffic to public directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
